# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |_repo| "https://github.com/#{repo}.git" }

ruby '3.2.1'

gem 'concurrent-ruby', '~> 1.2', require: 'concurrent'
gem 'concurrent-ruby-edge', '~> 0.7', require: 'concurrent-edge'
gem 'logger', '~> 1.5'

gem 'rubocop', '~> 1.55'
